# 部署台账（NAS 极空间 Q4）

> 每次用户手动部署后更新：日期 / 版本 / 验证结论 / 备注。
> 0.10.0 注：开发主线 = 本机 Windows 原生（无部署动作）；本台账记录 NAS docker
> 可选发布物的部署历史。
> 部署方式：`docker compose pull && docker compose up -d`（面板级回滚：环境变量 `WEBUI_UI=legacy`）。

| 日期 | 版本 | 部署人 | 验证结论 | 备注 |
|---|---|---|---|---|
| 2026-09-13 | 0.10.38 | 代理 | ✅ **驾驶舱改版实机部署 + 两批 19 项复验全绿**（`/vol1/stockdb`，本地构建）：**契约批 10/10**——`snapshot.assets` 真身（研究库 metrics=60/series=2/lists=12/snapshots=892/1.0 MB、双备份 12+14 份各带最新时间、磁盘分层 23.9G+89.6M+1.0M+0.1M）；`timeline` 日级新增 needs_action/awaiting/action_hint；**09-07 逐条分类 self_healed×3 / awaiting_mirror×6 / run_interrupted×1、needs_action=False**（改版起因：那条被部署重启打断的手动重试此前被当成「需处理」）；09-11 首条 self_healed + reason 透出；SPA 新产物在镜像内（Cockpit-*.js 含 需处理/一键重试/次成功/行情底座/分析数仓/未监控）；diag all_ok=true、webui=0.10.38。**动作批 9/9**——告警静音闭环（设 1h → overview.alerts 与 /api/alerts/summary 同步反映 → 解除幂等 → 非法预设 400）、补录三入口与覆盖后果说明在产物内、仓库/打板端点可达。 | 改版按用户设计稿 + 我方对数据真身的核对落地（设计稿 mydb 卡的「11 张基础数据表/连接池 2-10/无坏块」**无数据支撑已剔除**，改为研究库真实计数，取不到即显示「未监控」）。实机抓到并修掉 5 个问题：① 分类器被 warn 吞掉 reason 性质（09-07 每条都带 warn，3 条 reason 是认证失败/1 条是打断）；② 打断判定要求「非当日最后一条」导致 09-07 21:58 走不到该分支；③ `lights.test.ts` 时区依赖（容器 TZ=UTC 下必挂，且此前从未被 Vitest 收集）；④ 部署清单漏 `interfaces/web/routes.py` → 静音端点 404（清单已改按目录同步）；⑤ 版本号漏 bump。动作语义：立即补录=下拉三选项（同步/仓库补沉淀/打板回填 60 天，后者明说会覆盖已采集值）；告警静音=1h/4h/today 或自定义分钟，**只降提醒强度不改事实**（count 恒定、到期自动解除）。**只降提醒强度不改事实**（count 恒定、到期自动解除）。**最终整体复验 12/12 全绿**（老修复未退化 + 新改版生效；含回填 daily 恢复 60/60、快车道 0.03s、上游告警自愈闭环、diag 不再被网络条件打红）。 |
| 2026-09-13 | 0.10.37 | 代理 | ✅ **实机部署 + 10/10 复验通过**（`/vol1/stockdb`，本地构建 0.10.36→0.10.37）：① **A** `image.tag` 由 `null` 变为 **`0.3.5`**（Dockerfile `ENV IMAGE_TAG=<ARG VERSION>` 注入生效）；② **B** `/api/version` 新增 `engine` 块并读到真实运行版本 **`0.3.5-stockdb`**（`source=binary`——引擎无版本接口、`log.txt` 只落 ERROR 级，故扫二进制字面量；本机日志有横幅时优先走日志）；③ **B** 判定改同类版本线后：真实数据 `kind=up_to_date`（上游 测试版本0.3.5 vs 引擎 0.3.5）；受控注入上游 0.3.6 → **`update_available`** 且文案「建议升级镜像（重新 pin ARG VERSION + SHA256 后重建）」，回归护栏确认面板版本 0.10.x 不再参与比较（旧逻辑在此恒 `false`）；④ **D** 探针失败 → `kind=probe_failed`、`/api/version.msg` 显式标注「上游版本探测失败…本次无法判断是否有新版」；`/api/diag` 的 upstream note 由「最新 release：<tag>」改为「<tag>｜<判定说明>」+ env 新增 `engine_version`；看门狗链路实测 `upstream_status()=up_to_date` → `upstream_release_alert()=up_to_date` → 告警 0 条（不误报、条件恢复即撤警）。健康面：`diag all_ok=true`、container healthy（engine ready ~6s）、数据 `20260911` | 根因：判定取 `IMAGE_TAG` 与上游引擎 tag 比较，而 **Dockerfile 从未注入 IMAGE_TAG** → 容器内恒 None、回落到 `WEBUI_VERSION` → `(0,3,6) > (0,10,36) = False`，上游发新版永远判「不落后」；且探针失败静默留空、无任何告警。部署动作：SFTP 同步 15 文件（备份 `src-backup-20260913-0.10.37.tgz`）→ `docker compose build`（SPA 层缓存命中）→ `up -d`。部署坑留痕：① `ENV IMAGE_TAG` 首次误写在首个 `FROM` 之前 → `failed to solve: no build stage`，须放进最终 stage；② PowerShell `Set-Content -Encoding utf8` 给部署脚本加 BOM 并把中文注释压行 → install 步骤静默未执行（文件看似同步实则未更新），**改用 Python 写 ASCII-only 部署脚本**。数据面未动 |
| 2026-09-13 | 0.10.36 | 代理 | ✅ **SSH 巡检 + 实机部署 + 逐项复验**（`/vol1/stockdb`，本地构建）：**7/7 复验通过**，部署后 14 次 MCP 调用零失败。① `get_mydb_data` 由 **0% 成功率**（9 次全 INTERNAL_ERROR）变为 8/8 成功、`elapsed_ms` 0~1ms（`竞价快照:20260911` 读回 33 条、`打板序列:premium_mean` 16 值）；② `get_board_open_effect_history` 由每次 **40.4s**（`precomputed_days: 0`、`fallback_reason: 区间交易日无预计算结果`）变为 **0.02s**（`cache_hit: true`、`load_path: mydb`、`precomputed_days: 5`）；③ 版本号三处一致 **0.10.36**（`/api/version` + MCP `initialize.serverInfo` + `get_data_status`，此前硬编码报 0.10.7）；④ 看门狗首轮巡更（启动 +32s）**自动撤回** 09-11 两条残留告警（`alerts.json` → `[]`，无需人工清理）。健康面：`diag all_ok=true`、`latest=20260911`、`lag_days=0`、container healthy（engine ready ~6s） | 根因三条：① `research_store` 单例连接缺 `check_same_thread=False` → webui/MCP 的 `ThreadingHTTPServer` 跨线程读必炸（写路径因持锁复用连接反而正常，故指标一直正确落盘、坏的只有读）；② `SERVER_VERSION` 硬编码 `"0.10.7"`（原注释即「手工对齐」）漂移 28 版；③ 告警只投不撤（数据 17:50 追平，16:23 的「滞后 35 天」仍挂面板）。**连带修复**：研究库跨线程失败使 board 工具第三次全市场重算（40s）并连带拖慢 `get_data_status`（旧容器实测平均 18s；新容器实测 0.025s/0ms/0ms）。部署动作：SFTP 同步 10 个文件（备份 `src-backup-20260913-0.10.36.tgz`，`install -m` 保住执行位）→ `docker compose build`（SPA 层缓存命中，40s）→ `up -d`。数据面未动（无 schema 变更、数据卷未触碰）。**随后执行打板序列 60 天回填**（用户拍板：优先让分位可用）：`POST /api/auction/run {"task":"backfill","days":60}` → 80s 完成（backfilled_days=60，series premium_mean/success_rate 各 60 值）；metrics 由 18 天扩到 60 天（20260622~20260911），**序列与逐日指标 60/60 位一致（0 处不一致）**；分位链路实测可用（序列 60 观测 → `premium_mean` rank=0.383/neutral、`success_rate` rank=0.067/weak）。当日真采集值复核：**09-11 未变**（premium_mean=0.00897143/n_samples=33 逐位相同，仅 value_source 由 auction→kline）；09-09 由 13 只修正为 66 只（当日 09:26 采集不完整，唯一明显偏少天，回填用全市场日K 权威口径修正）；审计：08-17~09-04 有快照的天快照数≈n_samples（±1，清单在采集后微调），历史无快照日全部为 kline 口径 |
| 2026-09-10 | 0.10.35 | 代理 | ✅ **实机部署**（SSH /vol1/stockdb，本地构建）：打板采集取错字段 + 晚间重跑修复。实机复验：重启后**采集未再触发**（`fetched_at` 保持 21:31/21:56 不变——核心修复生效）；腾讯今开字段取第 5 位正确（600000 parsed_open=9.22=今开，≠field3 当前价 9.35）；`auction_fired` 守卫落盘；**停止误导告警**：清空 54 条历史告警（含 37 条竞价/开盘假偏差）；数据完整（近 5 日 5173~5177） | 根因：采集源第 3 位是**当前价**（注释误标今开）+ 采集窗口无上界 + 守卫纯内存（重启清空）→ 晚间重启采到收盘价冒充开盘价，对账全红。行情/仓库数据未受损；打板指标 `value_source=kline` 未被污染 |
| 2026-09-10 | 0.10.34 | 代理 | ✅ **实机部署 + 实测提速**（SSH /vol1/stockdb，本地构建）：仓库写入改 CSV 批量导入（替代 DuckDB `executemany` 逐行绑定）。部署后实测（5178 行）：`write_daily` **0.313s**（原 16.7s，~53×）、`write_codes` **0.068s**（原 23.5s，~345×）。回补/日常沉淀/周月聚合全受益 | 同批数据修复：0907/0908/0909 缺失 + 0910 残缺已 backfill 补齐（逐日 5173~5176）；0908 曾被坏逻辑误标 `empty:` 致跳过，删除标记后补回 |
| 2026-09-10 | 0.10.33 | 代理 | ✅ **实机部署 + 数据修复完成**：新引擎批量 SDK 区间语义修复（同日区间 + 旧语义回落）。实机：09-04 全市场快照由 ~50 只恢复 **5177**；backfill 补齐 0907/0908/0909/0910 四日（各 5173~5176 行，reconcile_ok=True）+ 一个 5 月历史空洞（0522，5180 行）；`codes` 回 5176、sedimented 79 天、health ok `20260910` | 残缺 0910 分区备份 `/vol1/stockdb/backup-partial-0910/`；脏 0907/0909 备份 `/vol1/stockdb/backup-dirty-0907-0909/`；回补耗时暴露写入瓶颈 → 0.10.34 |
| 2026-09-10 | 0.10.32 | 代理 | ✅ **实机部署（SSH /vol1/stockdb，本地构建）**：entrypoint 等待上限 60s→300s。实机复验「引擎 ready (after ~8s)、health 一路 healthy、零熔断零误报」——启动竞态已消（对比 0.10.31 冷加载 80~110s 超 60s 触发熔断） | 0.10.31/0.10.32 两版连部署，数据卷不动 |
| 2026-09-10 | 0.10.32 | 代理 | 🔧 **entrypoint 等待上限 60s→300s（待部署）**：0.10.31 实机部署发现启动竞态未完全消除——fnOS 引擎冷加载 22GB leveldb 约 80~110s，超 0.10.30 设的 60s 上限 → 重启后 21:17:38 探针失败 ×3 触发熔断 300s（暗灯 + 误报「行情数据不可用」+ 调度追赶连接被拒）。上限提 300s（就绪即 break） | 部署动作：fnOS 本地构建 `stockdb-ai:0.10.32` → `docker compose up -d`；复验重启后无 5 分钟暗灯/误报 |
| 2026-09-10 | 0.10.31 | 代理 | ✅ **实机部署成功（SSH /vol1/stockdb，本地构建）**：src 由 fd3b936 更新至 c5f0a4d（**丢弃现场调试残渣**：free_stockdb.py 的 stderr 诊断打印 + CRLF；恢复 entrypoint/dev.sh 执行位——fnOS 卷丢执行位老坑再现），compose tag 0.10.29→0.10.31，`docker compose build` 成功（`/tmp/pkg.tar: OK` 证明新 pin 正确）+ `up -d`。复验：webui 0.10.31、MCP get_stock_list 7563 条、get_kline 真实数据、health ok `20260910`、latency 7ms、diag 4/5（上游 GitHub 因网络受限降级，非故障）；熔断 300s 后自愈。观察：引擎冷加载 22GB≈80~110s，0.10.30 的 60s 等待不足 → 见 0.10.32 | 本批同发 0.10.30（MCP 取址）+ 0.10.31（重 pin）；现场调试残渣备份 `/vol1/stockdb/src-local-backup-20260910.patch` |
| 2026-09-10 | 0.10.30 | 代理 | ✅ **随 0.10.31 部署生效**：MCP 引擎取址分叉修复（`_base_url()` 内嵌走 config + Dockerfile `ENV STOCKDB_HOST=127.0.0.1`）。实机复验：修复前同 `get_stock_list` 超时 15005ms，修复后 7563 条毫秒级返回——主故障已消 | 详见 0.10.31 行 |
| 2026-09-07 | 0.10.29 | 代理 | 🔧 **引擎 0.3.5 协议适配（部署中）**：部署 0.10.28 后发现 0.3.5 引擎 HTTP 响应改 MsgPack（webui/MCP JSON 解析全失配：health/coverage/code_stats 变 None、同步完整性验证失败）→ msgpack_lite 解包器 + fetch 嗅探适配（0.10.29）；同步失败直接根因另 = /data/sync_url.txt 被 09-06 迁移期改为单行 workbuddy.link（该源 404）+ data/data/disable（首次全量完成标记，设计内保留）→ 已恢复默认双行（ah + ad always，备份 sync_url.txt.bak-20260907）。上游数据流 09-07 晚间通道不稳（manifest 502/网络不通，与迁移期吻合），同步最终结果待通道恢复后复验 | 部署动作：fnOS 本地构建 `stockdb-ai:0.10.29` → `docker compose up -d`（数据卷不动）；部署坑再验：git clone 到 fnOS 卷丢执行位（chmod +x entrypoint.sh 后重建） |
| 2026-09-07 | 0.10.28 | 代理 | ⚠️ **引擎升级已部署（0.3.2→0.3.5），部署中发现 0.3.5 协议变更**：/api/version 0.10.28、diag 5/5、pybao 0.3.5 可导入；但 health.latest/coverage 变 None（MsgPack 失配，0.10.29 修复）；同步器 0.3.5 设备校验通过（状态:有效），manifest 因通道 502 失败 | 上游删除 0.3.2 release 资产（404）→ 无法回退旧 pin；引擎镜像 tag = 面板版本 0.10.28；/data/sync_url.txt 已恢复双行默认（原单行 workbuddy.link 为 09-06 迁移期回归） |
| 2026-09-06 | 0.10.27 | 代理 | ✅ **webui「从零四件套」落地**（docs/design/webui-from-zero.md）：/api/snapshot 单通道五块聚合（totals 真数据 73 交易日/14 周/3 月/7 备份）；token 门禁实测全过（/api 无头 401、带头 200、静态壳 200、/mcp 豁免 200）；TypeScript 全量 + vue-tsc 进 build 门；浏览器目测 TokenGate 登录流 + 四灯全绿 + 时间线/资产卡正常 | Python 106 / Vitest 68 / vue-tsc 0 错；WEBUI_TOKEN 注入 NAS /vol1/stockdb/.env；部署坑留痕：①tar 覆盖解压会残留旧 .js 与新 .ts 冲突（必须先清空 src 再解压）；②sed -i 去 CRLF 会丢执行位（sed 后必须补 chmod +x）|
| 2026-09-06 | 0.10.20 | 代理 | ✅ **W1 webui 单页驾驶舱重设计落地**（定义书 docs/design/webui-cockpit-redesign.md，四批 #138~#141）。fnOS 本地构建部署：版本/diag/health 全绿，RSS 46MB、CPU 3.3%；/api/timeline 真数据七交易日（沉淀 5177~5182 行/日、每日备份、历史告警如实着色）；浏览器目测四灯全绿（同步下次 15:50）、时间线/抽屉/热更新确认流通过 | 导航收敛为 驾驶舱+数据同步 两页；数据同步保留独立页（定义书 v0.1 留痕）|
| 2026-09-06 | 0.10.18 | 代理 | ✅ **部署主体迁移：飞牛 fnOS 转正，极空间退役**。fnOS 本地构建（不经 CI）+ Windows 引擎数据推送（字节级对账）+ 极空间数据迁移（mydb 四表/warehouse/backups/research 逐项一致）；diag 5/5、同步链路全绿；调度非交易日忙转 bug 修复（101%→4.3% CPU）。Tailscale 100.66.1.5（节点小宝）/ 局域网 192.168.50.146 | compose /vol1/stockdb/；极空间实例同日删除；Windows 桌面留有极空间数据终备份 |
| 2026-08-30 | 0.10.17 | 用户 | ✅ 部署并完成旧分区迁移收口：删 0.10.6 时代 4 个 11 列分区 → backfill 重写 0819~0828 共 8 个交易日（26 列 schema、逐日对账零差异）→ W35 周K自动级联落盘（5185 行）/ 8 月不完整被守卫正确跳过 / ta_ma(5) 实测数值正常 / warehouse_run 新语义补写 anchor 0828 成功。**验收周收官签字（09-04）**：4 交易日全绿（15:50 同步准点 / 打板收口零偏差 / 沉淀对账零差异 / 日备份落盘）+ 晚到自愈三钩子 09-03 实战通过 + W36 级联 + 57 组周/月异源对账 0 差异 + 60 天 backfill 试跑（6/7/8 月首根真月K落位，仓库地板 0819→0526） | 0.10.13~0.10.17 五连修收官；复权物化列 NULL 属设计内（SDK 因子通道未接，延后项）；v_week_current 周日显示上周区间为周期边界已知瑕疵 |
| 2026-08-29 | 0.10.13 | 用户 | ✅ 部署并远程复验通过：version=0.10.13 / diag 5 项全绿 / RSS 49MB（新基线）/ 数据 20260828 最新；MCP get_money_flow 实测可用（source=sdk，600633 当日主力净额正确）；get_kline 正常；调度 next=08-31 15:50 | 首批含 0.10.7~0.10.13（storage 收口 + 周月聚合物化 + 仓库日备份 + 数据晚到自愈三钩子）；镜像 digest 463d6484…；⚠️ get_money_flow 的 count 参数实测未生效（返回全历史，契约已记 data-source.md）；周一首次沉淀将自动触发周/月聚合物化（预期变慢）；0.10.7~0.10.13 批次验收周 = 08-31~09-04 |
| 2026-08-27 | 0.10.6 | 用户 | ✅ 部署（2026-08-27 11:46 启动）；试运行 2 个交易日（08-27/08-28）发现数据晚到无自愈缺口（08-28 实证，0.10.13 修复），验收周未走完即被 0.10.13 替换 | 台账补记（部署时未登记）；0.10.6 验收项并入 0.10.13 批次验收周 |
| 2026-08-16 | 0.8.15 | 用户 | ✅ 部署并回填 60 天；终验 60×3 零不一致；修复生效实证（20260615 n 63→84、20260701 n 122→140，找回真涨停）；08-14 基准 47/0.012113/0.4894 不变 | 涨停判定参考价改 lag close（0.8.14 验收驳回修正）；新清单 recovered 259 / removed 73 已导出待命理档案复验 |
| 2026-08-16 | 0.8.14 | 用户 | ⚠️ 部署并回填，但被命理档案验收驳回（严重不通过）：513/517 条剔除为误删（污染不均匀，统一因子反推错误） | 0.8.14 教训：污染不均匀，禁止统一反推；正确方案 = 普通日 lag close + 除权日 pre_close（0.8.15 实施）；清单已作废 |
| 2026-08-16 | 0.8.13 | 用户 | ✅ 部署并终验通过：60 天 × 3 项（样本数/均值/成功率）与生产工具 get_board_open_effect_history 交叉核对 **0 不一致**；08-14 存储 = 47 / 0.012113 / 0.489362 与用户 SQL 基准逐位一致 | 溢价分母 = T-1 收盘价（除权日口径修复）；至此存储数据 = 生产工具 = 用户公式，三口径完全同源 |
| 2026-08-16 | 0.8.12 | 用户 | ✅ 部署并复验通过（version=0.8.12；回填 60 天；08-14=47/0.012113/0.4894 命中用户 SQL 基准）；交叉核对发现 9 天除权日均值偏差 → 0.8.13 修复 | 涨停样本口径对齐用户 SQL（精确封板+ST/北交所剔除，与生产 board_metrics 同源）；ST 规则变更确认后维持剔除（用户拍板） |
| 2026-08-16 | 0.8.11 | 用户 | ✅ 部署并复验通过：version=0.8.11 / 回填 60 天 / 序列 60/60 / strength_60d 字段 60/60 载荷全携带 / 历史日 rank 全 null（新口径预期，分母池就绪） | 分位改用户公式（严格小于/固定60分母）+ 强弱标签；首个真实分位与强弱标签 = 08-17 09:26 采集日 |
| 2026-08-16 | 0.8.10 | 用户 | ✅ 部署并复验通过：version=0.8.10 / diag 5 检查全绿 / rss_mb=47 基线（遥测上线）/ MCP get_mydb_data 读回正常（序列 2 键仍在，mydb 数据跨版本留存） | rd 单连接加固（锁+自愈+归一化+RSS 遥测）；188 测试全绿；周一 08-17 09:26 首次自动采集 + 16:30 收口将跑在加固后代码 |
| 2026-08-16 | 0.8.9 | 用户 | ✅ 部署并回填验证通过：回填 60 天全绿（backfilled_days=60，序列 60/60）；MCP 读回逐项核对（序列 len=60×2、逐日指标 60 个 dict 全非空、rank_60d 首日 None 无未来函数） | ⚠️ 首次"已部署 0.8.9"实为 0.8.8（镜像未构建）；补触发 workflow 构建后二次部署成功。随后 /api/data/read+tables 并发探测触发全进程冻结 + NAS 内存 3.8GB，重启恢复 → 根因 0.8.10 修复 |
| 2026-08-15 | 0.6.0 | 用户 | ✅ 体验正向（"丝滑"），发现多标签切页崩溃 | /legacy 回滚通道在线验证通过（代理侧） |
| 2026-08-15 | 0.6.1 | 用户 | ⚠️ 部署成功，反馈"点一点易崩溃" | 触发 0.6.2→0.6.6 稳定性专项 |
| 2026-08-15 | 0.6.6 | 用户 | ✅ 用户确认部署；代理侧远程复验通过（version=0.6.6 / health ok lag=0 / MCP 契约信封完整） | 稳定性三件套 + 空载荷防线；观察期 20260817 08:45 武装就绪 |
| 2026-08-15 | 0.8.0 | 用户 | ✅ 部署并远程复验通过（version=0.8.0 / overview 无 paper / diag 5 检查全绿 / auction 端点降级正常 / paper 路由 404 / MCP 12 工具可用 / 深链全 200） | 砍掉模拟盘 + 打板采集修复；周一 09:26 首跑；遗留 paper.sqlite3 未删、待用户自行处理 |
| 2026-08-15 | 0.8.6 | 用户 | ✅ 部署并读回对账通过：回填 47 天真实落库（序列 dict 47 值 + 每日指标含 rank_60d），服务全程健康 | 0.8.2→0.8.6 五项修复（异步/全量/节流×2/存值类型）；周一 09:26 首跑分位分母就绪 |
| 待部署 | 0.8.0 | — | 待部署 | 移除模拟盘（数据基座收敛）；NAS 上 paper.sqlite3 / mx_apikey.txt 保留未用，用户可自行删除 |

## 回滚记录

（暂无）
